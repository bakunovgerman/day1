# Day1 - AI Chat Agent

Простое Android приложение для общения с AI агентом через OpenRouter API.

## Возможности

- 💬 Простой чат-интерфейс
- 🤖 Интеграция с OpenRouter API (модель GPT-4o-mini)
- 🎨 Современный Material Design 3 UI
- ⚡ Использование Ktor для HTTP запросов
- 📝 История сообщений
- 🔄 Индикация загрузки
- ❌ Обработка ошибок

## Технологии

- **Kotlin** - язык программирования
- **Jetpack Compose** - UI фреймворк
- **Ktor Client** - HTTP клиент для работы с API
- **Coroutines & Flow** - асинхронность и реактивность
- **ViewModel** - архитектура MVVM
- **Kotlinx Serialization** - сериализация JSON

## Установка и запуск

### Требования

- Android Studio Hedgehog или новее
- JDK 11 или новее
- Android SDK 24 или выше
- OpenRouter API ключ (получить на [openrouter.ai](https://openrouter.ai))

### Шаги установки

1. Клонируйте или скачайте проект
2. Откройте проект в Android Studio
3. **Настройте API ключ** одним из способов:
   
   **Вариант 1: Локальная разработка (рекомендуется)**
   - Откройте файл `local.properties` в корне проекта
   - Добавьте строку: `OPENROUTER_API_KEY=your-api-key-here`
   - Замените `your-api-key-here` на ваш реальный ключ
   
   **Вариант 2: CI/CD / GitHub Actions**
   - Добавьте переменную окружения `OPENROUTER_API_KEY` в настройки вашего CI/CD
   - Для GitHub: Settings → Secrets and variables → Actions → New repository secret
   - Имя: `OPENROUTER_API_KEY`, значение: ваш ключ
   
4. Синхронизируйте Gradle файлы (Build > Sync Project with Gradle Files)
5. Запустите приложение на эмуляторе или устройстве

## Использование

1. API ключ автоматически загружается из конфигурации при сборке
2. Начните общение с AI агентом, вводя сообщения в поле ввода
3. Используйте кнопку "Очистить чат" для начала новой беседы
4. Если API ключ не настроен, в приложении появится соответствующее сообщение

## Структура проекта

```
app/src/main/java/com/example/day1/
├── MainActivity.kt              # Главная активность
├── data/
│   └── ChatMessage.kt          # Модели данных
├── api/
│   └── OpenRouterService.kt    # Сервис для работы с API
├── viewmodel/
│   └── ChatViewModel.kt        # ViewModel для управления состоянием
└── ui/
    ├── ChatScreen.kt           # UI чата
    └── theme/                  # Тема приложения
```

## API OpenRouter

Приложение использует OpenRouter API для общения с AI моделями.

### Endpoint
```
POST https://openrouter.ai/api/v1/chat/completions
```

### Headers
```
Authorization: Bearer <API_KEY>
Content-Type: application/json
HTTP-Referer: com.example.day1
X-Title: Day1 Chat App
```

### Request Body
```json
{
  "model": "openai/gpt-4o-mini",
  "messages": [
    {
      "role": "user",
      "content": "Ваше сообщение"
    }
  ]
}
```

## Зависимости

```kotlin
// Ktor Client
implementation("io.ktor:ktor-client-core:2.3.7")
implementation("io.ktor:ktor-client-android:2.3.7")
implementation("io.ktor:ktor-client-content-negotiation:2.3.7")
implementation("io.ktor:ktor-serialization-kotlinx-json:2.3.7")
implementation("io.ktor:ktor-client-logging:2.3.7")

// ViewModel
implementation("androidx.lifecycle:lifecycle-viewmodel-compose:2.6.1")

// Serialization
implementation("org.jetbrains.kotlinx:kotlinx-serialization-json:1.6.2")
```

## Особенности реализации

### Архитектура MVVM
- **Model**: Модели данных в `data/`
- **View**: Composable функции в `ui/`
- **ViewModel**: Логика в `viewmodel/`

### Управление состоянием
- Используется StateFlow для реактивного обновления UI
- Все сетевые запросы выполняются асинхронно через корутины

### Обработка ошибок
- Все ошибки API отображаются в UI
- Пользователь может закрыть уведомление об ошибке

### Безопасность API ключа
- API ключ встраивается в приложение на этапе сборки через BuildConfig
- Ключ загружается из `local.properties` (для разработки) или переменных окружения (для CI/CD)
- Файл `local.properties` исключен из Git через `.gitignore`
- Для production-сборок рекомендуется использовать дополнительное шифрование или backend-прокси

## GitHub Actions / CI/CD

Проект включает пример конфигурации GitHub Actions для автоматической сборки (`.github/workflows/build.yml`).

### Настройка GitHub Actions

1. Перейдите в Settings → Secrets and variables → Actions
2. Нажмите "New repository secret"
3. Добавьте секрет:
   - **Name**: `OPENROUTER_API_KEY`
   - **Secret**: ваш OpenRouter API ключ
4. При каждом push в main/master будет автоматически собираться APK

Готовый APK можно скачать из раздела Actions → Build → Artifacts.

## Будущие улучшения

- [ ] Сохранение истории чата в базу данных
- [ ] Поддержка отправки изображений
- [ ] Выбор различных AI моделей
- [ ] Темная/светлая тема
- [ ] Экспорт истории чата
- [x] Загрузка API ключа из переменных окружения

## Лицензия

MIT License
