# Сводка изменений: Механизм сжатия истории диалога

## ✅ Реализовано

### 1. Генерация summary через OpenRouterService
**Файл**: `app/src/main/java/com/example/day1/api/OpenRouterService.kt`

Добавлена функция `generateSummary()`:
- Использует модель `openai/gpt-4o-mini`
- Temperature = 1.0 для естественного результата
- Профессиональный system prompt для сжатия диалогов на русском языке
- User prompt запрашивает краткое резюме (2-4 предложения)

### 2. Логика сжатия контекста в ChatViewModel
**Файл**: `app/src/main/java/com/example/day1/viewmodel/ChatViewModel.kt`

Добавлено:
- `_useContextCompression: MutableStateFlow<Boolean>` - состояние включения/выключения
- `contextSummary: String?` - хранение сгенерированного резюме
- `toggleContextCompression()` - функция управления режимом

Обновлено:
- `sendMessage()` - проверка количества сообщений и генерация summary
- Логика формирования messageHistory:
  - Без сжатия: system prompt + вся история
  - Со сжатием: system prompt + summary + только текущее сообщение
- `clearChat()` - сброс summary при очистке

### 3. UI для управления сжатием
**Файл**: `app/src/main/java/com/example/day1/ui/ChatScreen.kt`

Добавлено в `ChatScreen`:
- Получение состояния `useContextCompression`
- Передача в `SystemPromptDialog`

Добавлено в `SystemPromptDialog`:
- Параметр `useContextCompression: Boolean`
- Параметр `onContextCompressionToggle: (Boolean) -> Unit`
- Switch для включения/выключения с описанием
- Информационное сообщение при включенном режиме

## 🎯 Ключевые особенности

### Алгоритм работы

```
1. Пользователь отправляет сообщение
   ↓
2. Сообщение добавляется в _messages.value
   ↓
3. Проверка: сжатие включено && размер % 11 == 0?
   ├─ ДА → Генерация нового summary всех сообщений
   └─ НЕТ → Пропустить
   ↓
4. Формирование messageHistory:
   ├─ Если summary существует:
   │  └─ system prompt + summary + текущее сообщение
   └─ Иначе:
      └─ system prompt + вся история
   ↓
5. Отправка в API
   ↓
6. Получение и сохранение ответа
```

### Экономия токенов

**Пример расчета:**

До сжатия (10 сообщений в истории):
- User: 50 токенов × 5 = 250
- AI: 200 токенов × 5 = 1000
- **Итого: ~1250 токенов в prompt**

После сжатия (12+ сообщений):
- Summary: ~150 токенов
- Текущее сообщение: 50 токенов
- **Итого: ~200 токенов в prompt**

**Экономия: ~84%** 🎉

## 📋 Что сохраняется

✅ Все сообщения видны в UI чата
✅ Полная история диалога доступна пользователю
✅ Прокрутка работает нормально
✅ Метрики (токены, время, стоимость) отображаются

## 🔄 Что изменяется

❌ В API отправляется только summary + текущее сообщение (после 10-го)
❌ Предыдущие сообщения не включаются в запрос
❌ AI видит только сжатый контекст

## 🎛️ Управление

### Включение сжатия
1. Настройки → "Сжатие контекста" → ВКЛ
2. Отправить 11 сообщений (5-6 пар вопрос-ответ)
3. Summary генерируется автоматически при 11-м сообщении
4. Каждые 11 сообщений генерируется новый summary

### Выключение сжатия
1. Настройки → "Сжатие контекста" → ВЫКЛ
2. Summary сбрасывается
3. Отправляется полная история

### Сброс
- Очистка чата → summary сбрасывается
- Выключение режима → summary сбрасывается

## 📝 Промпт для summary

**System:**
```
Ты - эксперт по сжатию диалогов. Твоя задача - создать краткое, 
лаконичное резюме диалога на русском языке, сохраняя ключевую 
информацию, контекст и важные детали. Резюме должно быть 
максимально сжатым, но информативным.
```

**User:**
```
Создай краткое резюме следующего диалога, выделив ключевые 
темы, решения и важную информацию:

[текст всех сообщений]

Резюме должно быть кратким (2-4 предложения) и содержать 
только самое важное.
```

## 🧪 Тестирование

Создан файл `TESTING_INSTRUCTIONS.md` с детальными инструкциями:
- Базовая функциональность
- Отключение сжатия
- Очистка чата
- Проверка UI
- Проверка метрик

## 📚 Документация

Создан файл `CONTEXT_COMPRESSION.md` с полным описанием:
- Принцип работы
- Технические детали
- Преимущества
- Инструкции по использованию

## ⚠️ Важные примечания

1. **Summary генерируется каждые 11 сообщений**
   - При достижении 11, 22, 33, 44 и т.д. сообщений
   - После 5-6 полных пар вопрос-ответ
   - Автоматически обновляется с новым контекстом

2. **Summary обновляется автоматически**
   - Пересоздается каждые 11 сообщений
   - Включает весь накопленный контекст
   - Сбрасывается при очистке или выключении

3. **Все сообщения остаются в UI**
   - Сжатие влияет только на API запросы
   - Пользователь видит полную историю
   - Локальное хранилище не затрагивается

4. **Режим можно включать/выключать в любой момент**
   - Переключение в настройках
   - Мгновенное применение
   - Сброс summary при выключении

## 🚀 Готово к использованию

Все изменения реализованы и готовы к тестированию. Функционал полностью соответствует требованиям:

✅ Сжатие каждые 11 сообщений (при 11, 22, 33 и т.д.)
✅ Хранение локально (в памяти приложения)
✅ Использование OpenRouterService для генерации
✅ Краткий лаконичный промпт на русском языке
✅ Summary как system prompt для последующих запросов
✅ Не отправляется весь диалог после получения summary
✅ Все сообщения видны в UI
✅ Переключатель в настройках
✅ Автоматическое обновление summary
